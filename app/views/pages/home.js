import React from "react";

export default React.createClass( {
    render( ) {
        return (
            <div>
                <h1>Welcome to React!</h1>
            </div>
        );
    },
} );
